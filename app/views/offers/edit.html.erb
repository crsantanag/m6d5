<% content_for :title, "Editing offer" %>

<% if notice.present? %>
  <p class="my-2">
    <span class="d-block notice" style="color: red"> <strong><%= notice %> </strong> </span>
  </p>
<% end %>

<h2>Editando oferta de cargo</h2>

<%= render "form", offer: @offer %>

<div>
  <%= link_to "Regresar", offers_path, class: "py-2 me-2 btn btn-light border border-2 rounded-2 border-secondary-subtle" %>
</div>
